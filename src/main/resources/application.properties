# ============================
# MongoDB Atlas Connection
# ============================
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.auto-index-creation=true

# ============================
# JWT Configuration
# ============================
app.jwt.secret=${JWT_SECRET}
app.jwt.expirationMs=86400000

# ============================
# Google OAuth2 Configuration
# ============================
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri=https://api.talentsynctech.in/login/oauth2/code/google

spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=sub



# ============================
# CORS Configuration
# ============================
spring.web.cors.allowed-origins=https://www.talentsynctech.in,https://talentsynctech.in
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# ============================
# Frontend URL
# ============================
app.frontend.url=https://www.talentsynctech.in

# ============================
# Server
# ============================
server.port=8080

# ============================
# Logging — PRODUCTION LEVELS
# ============================
# Framework packages: WARN — only real errors surface (was DEBUG, extremely chatty)
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN
logging.level.org.springframework.web.client.RestTemplate=WARN
logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=WARN
# Application code: INFO — startup messages + important events visible
logging.level.com.smartresume=INFO

# ============================
# Health Check (TYPE THESE)
# ============================
management.endpoints.web.exposure.include=health,info
management.health.mongo.enabled=false
management.health.mongodb.enabled=false

# Email Configuration (Resend API)
# SMTP is blocked on Render free tier, using HTTP API instead
# ============================
# MongoDB Atlas Connection
# ============================
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.auto-index-creation=true

# ============================
# JWT Configuration
# ============================
app.jwt.secret=${JWT_SECRET}
app.jwt.expirationMs=86400000

# ============================
# Google OAuth2 Configuration
# ============================
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri=https://api.talentsynctech.in/login/oauth2/code/google

spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=sub



# ============================
# CORS Configuration
# ============================
spring.web.cors.allowed-origins=https://www.talentsynctech.in,https://talentsynctech.in
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# ============================
# Frontend URL
# ============================
app.frontend.url=https://www.talentsynctech.in

# ============================
# Server
# ============================
server.port=8080

# ============================
# Logging — PRODUCTION LEVELS
# ============================
# Framework packages: WARN — only real errors surface (was DEBUG, extremely chatty)
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN
logging.level.org.springframework.web.client.RestTemplate=WARN
logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=WARN
# Application code: INFO — startup messages + important events visible
logging.level.com.smartresume=INFO

# ============================
# Health Check (TYPE THESE)
# ============================
management.endpoints.web.exposure.include=health,info
management.health.mongo.enabled=false
management.health.mongodb.enabled=false

# Email Configuration (Resend API)
# SMTP is blocked on Render free tier, using HTTP API instead
resend.api.key=${RESEND_API_KEY}
resend.from.email=${RESEND_FROM_EMAIL:onboarding@resend.dev}

# ============================
# ML Service Configuration
# ============================
# IMPORTANT: Update this URL after deploying ML to Hugging Face Spaces
# Format: https://<your-hf-username>-talentsync-ml.hf.space
ml.service.url=${ML_SERVICE_URL:http://localhost:5000}
# If your Hugging Face Space is PRIVATE, provide your HF Access Token here
hf.api.token=${HF_TOKEN:}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=200MB